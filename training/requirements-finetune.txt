# =============================================================================
# requirements-finetune.txt — added on top of nvcr.io/nvidia/pytorch base
# Base already provides: torch 2.6, CUDA 12.6, basic transformers
#
# No version pins — let pip resolve compatible versions.
# Key constraint: unsloth requires trl<=0.15.2; pinning trl>=1.x breaks it.
# =============================================================================

unsloth
unsloth_zoo
peft
trl
bitsandbytes
datasets
accelerate
sentencepiece
protobuf
einops
wandb
requests
